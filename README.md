# Flask-HTML-api-to-sqlite
A web-based (local, but displayed in browser) Flask-HTML interface (API) allowing to quickly execute SQL commands to query a sqlite DB with a user friendly user interface


